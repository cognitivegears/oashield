
# deleteOrder: DELETE /store/order/{orderId}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/store/order/(?:[^/]+)$" "id:4200323,phase:2,pass,nolog,skipAfter:END_deleteOrder"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within DELETE" "id:4200324,phase:2,pass,nolog,skipAfter:END_deleteOrder"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200335,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200346,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_deleteOrder"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200342,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_deleteOrder

## The request passed all checks
SecAction "phase:2,allow:request,id:4200343"

## End of checks for this operation
SecMarker END_deleteOrder

# getInventory: GET /store/inventory
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/store/inventory$" "id:4200363,phase:2,pass,nolog,skipAfter:END_getInventory"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200364,phase:2,pass,nolog,skipAfter:END_getInventory"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200375,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200386,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_getInventory"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200382,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_getInventory

## The request passed all checks
SecAction "phase:2,allow:request,id:4200383"

## End of checks for this operation
SecMarker END_getInventory

# getOrderById: GET /store/order/{orderId}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/store/order/(?:[0-9]{1,19})$" "id:4200403,phase:2,pass,nolog,skipAfter:END_getOrderById"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200404,phase:2,pass,nolog,skipAfter:END_getOrderById"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200415,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200426,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_getOrderById"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200422,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_getOrderById

## The request passed all checks
SecAction "phase:2,allow:request,id:4200423"

## End of checks for this operation
SecMarker END_getOrderById

# placeOrder: POST /store/order
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/store/order$" "id:4200443,phase:2,pass,nolog,skipAfter:END_placeOrder"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200444,phase:2,pass,nolog,skipAfter:END_placeOrder"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:json\.id|json\.petId|json\.quantity|json\.shipDate|json\.status|json\.complete)$" "id:4200455,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4210817,phase:2,pass,nolog,skipAfter:ENDMEDIA_placeOrder_0"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4210818,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.id "!@rx ^[0-9]{1,19}$" "id:4210822,phase:2,block,msg:'Invalid value for property json.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.petId "!@rx ^[0-9]{1,19}$" "id:4210834,phase:2,block,msg:'Invalid value for property json.petId',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.quantity "!@rx ^[0-9]{1,19}$" "id:4210846,phase:2,block,msg:'Invalid value for property json.quantity',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.shipDate "!@rx ^(\d{4}-((01|03|05|07|08|10|12)-(0[1-9]|1\d|2\d|3[0-1])|(04|06|09|11)-(0[1-9]|1\d|2\d|30)|02-(0[1-9]|1\d|2\d))T([01]\d|2[0-3]):([0-5]\d):([0-5]\d)(\.\d+)?([Zz]|[+\-](0\d|1[0-4])(:[0-5]\d)?))$" "id:4210858,phase:2,block,msg:'Invalid value for property json.shipDate',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.status "!@rx ^(placed|approved|delivered)$" "id:4210870,phase:2,block,msg:'Invalid value for property json.status',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.complete "!@rx ^(true|false)$" "id:4210882,phase:2,block,msg:'Invalid value for property json.complete',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210820,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_placeOrder"

SecMarker ENDMEDIA_placeOrder_0


# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200462,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_placeOrder

## The request passed all checks
SecAction "phase:2,allow:request,id:4200463"

## End of checks for this operation
SecMarker END_placeOrder
