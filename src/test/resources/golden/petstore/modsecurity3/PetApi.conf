
# addPet: POST /pet
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet$" "id:4200002,phase:2,pass,nolog,skipAfter:END_addPet"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200003,phase:2,pass,nolog,skipAfter:END_addPet"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:json\.id|json\.category|json\.category\.id|json\.category\.name|json\.name|json\.photoUrls|json\.photoUrls\.(?:array_)?\d{1,9}|json\.tags|json\.tags\.(?:array_)?\d{1,9}|json\.tags\.(?:array_)?\d{1,9}\.id|json\.tags\.(?:array_)?\d{1,9}\.name|json\.status)$" "id:4200014,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4210001,phase:2,pass,nolog,skipAfter:ENDMEDIA_addPet_0"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4210002,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.id "!@rx ^[0-9]{1,19}$" "id:4210010,phase:2,block,msg:'Invalid value for property json.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.category.id "!@rx ^[0-9]{1,19}$" "id:4210022,phase:2,block,msg:'Invalid value for property json.category.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.category.name "!@rx ^[a-zA-Z0-9]+[a-zA-Z0-9\.\-_]*[a-zA-Z0-9]+$" "id:4210034,phase:2,block,msg:'Invalid value for property json.category.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS:json.name "@eq 0" "id:4210045,phase:2,block,msg:'Missing required property json.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.name "!@rx ^.+$" "id:4210046,phase:2,block,msg:'Invalid value for property json.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.photoUrls\.(?:array_)?\d{1,9}$/ "!@rx ^.+$" "id:4210058,phase:2,block,msg:'Invalid value for property json.photoUrls',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.tags\.(?:array_)?\d{1,9}\.id$/ "!@rx ^[0-9]{1,19}$" "id:4210070,phase:2,block,msg:'Invalid value for property json.tags.0.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.tags\.(?:array_)?\d{1,9}\.name$/ "!@rx ^.+$" "id:4210082,phase:2,block,msg:'Invalid value for property json.tags.0.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.status "!@rx ^(available|pending|sold)$" "id:4210094,phase:2,block,msg:'Invalid value for property json.status',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210004,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_addPet"

SecMarker ENDMEDIA_addPet_0
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/xml" "id:4210005,phase:2,pass,nolog,skipAfter:ENDMEDIA_addPet_1"
SecRule REQBODY_ERROR "!@eq 0" "id:4210006,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210008,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_addPet"

SecMarker ENDMEDIA_addPet_1


# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200021,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_addPet

## The request passed all checks
SecAction "phase:2,allow:request,id:4200022"

## End of checks for this operation
SecMarker END_addPet

# deletePet: DELETE /pet/{petId}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/(?:[0-9]{1,19})$" "id:4200042,phase:2,pass,nolog,skipAfter:END_deletePet"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within DELETE" "id:4200043,phase:2,pass,nolog,skipAfter:END_deletePet"

SecRule REQUEST_HEADERS:api_key "!@rx ^.*$" "id:4210202,phase:2,block,msg:'Forbidden header value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200054,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200065,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_deletePet"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200061,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_deletePet

## The request passed all checks
SecAction "phase:2,allow:request,id:4200062"

## End of checks for this operation
SecMarker END_deletePet

# findPetsByStatus: GET /pet/findByStatus
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/findByStatus$" "id:4200082,phase:2,pass,nolog,skipAfter:END_findPetsByStatus"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200083,phase:2,pass,nolog,skipAfter:END_findPetsByStatus"

SecRule ARGS_GET:status "!@rx ^(?:(available|pending|sold))(?:,(?:(available|pending|sold))){0,999}$" "id:4210230,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:status "@eq 0" "id:4210246,phase:2,block,msg:'Missing required parameter status',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:status)$" "id:4200094,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200105,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_findPetsByStatus"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200101,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_findPetsByStatus

## The request passed all checks
SecAction "phase:2,allow:request,id:4200102"

## End of checks for this operation
SecMarker END_findPetsByStatus

# findPetsByTags: GET /pet/findByTags
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/findByTags$" "id:4200122,phase:2,pass,nolog,skipAfter:END_findPetsByTags"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200123,phase:2,pass,nolog,skipAfter:END_findPetsByTags"

SecRule ARGS_GET:tags "!@rx ^(?:.+)(?:,(?:.+)){0,999}$" "id:4210270,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:tags "@eq 0" "id:4210286,phase:2,block,msg:'Missing required parameter tags',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:tags)$" "id:4200134,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200145,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_findPetsByTags"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200141,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_findPetsByTags

## The request passed all checks
SecAction "phase:2,allow:request,id:4200142"

## End of checks for this operation
SecMarker END_findPetsByTags

# getPetById: GET /pet/{petId}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/(?:[0-9]{1,19})$" "id:4200162,phase:2,pass,nolog,skipAfter:END_getPetById"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200163,phase:2,pass,nolog,skipAfter:END_getPetById"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200174,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type

SecAction "id:4200185,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_getPetById"

# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200181,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_getPetById

## The request passed all checks
SecAction "phase:2,allow:request,id:4200182"

## End of checks for this operation
SecMarker END_getPetById

# updatePet: PUT /pet
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet$" "id:4200202,phase:2,pass,nolog,skipAfter:END_updatePet"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within PUT" "id:4200203,phase:2,pass,nolog,skipAfter:END_updatePet"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:json\.id|json\.category|json\.category\.id|json\.category\.name|json\.name|json\.photoUrls|json\.photoUrls\.(?:array_)?\d{1,9}|json\.tags|json\.tags\.(?:array_)?\d{1,9}|json\.tags\.(?:array_)?\d{1,9}\.id|json\.tags\.(?:array_)?\d{1,9}\.name|json\.status)$" "id:4200214,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4210345,phase:2,pass,nolog,skipAfter:ENDMEDIA_updatePet_0"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4210346,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.id "!@rx ^[0-9]{1,19}$" "id:4210354,phase:2,block,msg:'Invalid value for property json.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.category.id "!@rx ^[0-9]{1,19}$" "id:4210366,phase:2,block,msg:'Invalid value for property json.category.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.category.name "!@rx ^[a-zA-Z0-9]+[a-zA-Z0-9\.\-_]*[a-zA-Z0-9]+$" "id:4210378,phase:2,block,msg:'Invalid value for property json.category.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS:json.name "@eq 0" "id:4210389,phase:2,block,msg:'Missing required property json.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.name "!@rx ^.+$" "id:4210390,phase:2,block,msg:'Invalid value for property json.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.photoUrls\.(?:array_)?\d{1,9}$/ "!@rx ^.+$" "id:4210402,phase:2,block,msg:'Invalid value for property json.photoUrls',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.tags\.(?:array_)?\d{1,9}\.id$/ "!@rx ^[0-9]{1,19}$" "id:4210414,phase:2,block,msg:'Invalid value for property json.tags.0.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:/^json\.tags\.(?:array_)?\d{1,9}\.name$/ "!@rx ^.+$" "id:4210426,phase:2,block,msg:'Invalid value for property json.tags.0.name',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.status "!@rx ^(available|pending|sold)$" "id:4210438,phase:2,block,msg:'Invalid value for property json.status',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210348,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_updatePet"

SecMarker ENDMEDIA_updatePet_0
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/xml" "id:4210349,phase:2,pass,nolog,skipAfter:ENDMEDIA_updatePet_1"
SecRule REQBODY_ERROR "!@eq 0" "id:4210350,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210352,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_updatePet"

SecMarker ENDMEDIA_updatePet_1


# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200221,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_updatePet

## The request passed all checks
SecAction "phase:2,allow:request,id:4200222"

## End of checks for this operation
SecMarker END_updatePet

# updatePetWithForm: POST /pet/{petId}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/(?:[0-9]{1,19})$" "id:4200242,phase:2,pass,nolog,skipAfter:END_updatePetWithForm"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200243,phase:2,pass,nolog,skipAfter:END_updatePetWithForm"

SecRule ARGS_POST:name "!@rx ^.*$" "id:4210542,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_POST:name "@gt 1" "id:4210543,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS_POST:status "!@rx ^.*$" "id:4210582,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_POST:status "@gt 1" "id:4210583,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:name|status)$" "id:4200254,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type
# requestBody is optional (the OAS3 default): a request without a body skips body checks
SecRule &REQUEST_HEADERS:Content-Type "@eq 0" "id:4200266,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_updatePetWithForm"
# form-urlencoded/multipart: text fields land in ARGS_POST on both engines and are
# validated by the parameter rules and ARGS_NAMES allowlist above
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/x-www-form-urlencoded" "id:4210489,phase:2,pass,nolog,skipAfter:ENDMEDIA_updatePetWithForm_0"
SecRule REQBODY_ERROR "!@eq 0" "id:4210490,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210492,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_updatePetWithForm"

SecMarker ENDMEDIA_updatePetWithForm_0


# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200261,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_updatePetWithForm

## The request passed all checks
SecAction "phase:2,allow:request,id:4200262"

## End of checks for this operation
SecMarker END_updatePetWithForm

# uploadFile: POST /pet/{petId}/uploadImage
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/v2/pet/(?:[0-9]{1,19})/uploadImage$" "id:4200282,phase:2,pass,nolog,skipAfter:END_uploadFile"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200283,phase:2,pass,nolog,skipAfter:END_uploadFile"

SecRule ARGS_POST:additionalMetadata "!@rx ^.*$" "id:4210666,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_POST:additionalMetadata "@gt 1" "id:4210667,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS_POST:file "!@rx ^.*$" "id:4210706,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_POST:file "@gt 1" "id:4210707,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:additionalMetadata|file)$" "id:4200294,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle request bodies by declared media type
# requestBody is optional (the OAS3 default): a request without a body skips body checks
SecRule &REQUEST_HEADERS:Content-Type "@eq 0" "id:4200306,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_uploadFile"
# form-urlencoded/multipart: text fields land in ARGS_POST on both engines and are
# validated by the parameter rules and ARGS_NAMES allowlist above
SecRule REQUEST_HEADERS:Content-Type "!@rx ^multipart/form-data" "id:4210613,phase:2,pass,nolog,skipAfter:ENDMEDIA_uploadFile_0"
SecRule REQBODY_ERROR "!@eq 0" "id:4210614,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4210616,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_uploadFile"

SecMarker ENDMEDIA_uploadFile_0


# Declared consumes exist but the request Content-Type matched none of them
SecAction "id:4200301,log,auditlog,block,phase:2,msg:'Unexpected content type'"


SecMarker AFTER_CONSUMES_uploadFile

## The request passed all checks
SecAction "phase:2,allow:request,id:4200302"

## End of checks for this operation
SecMarker END_uploadFile
