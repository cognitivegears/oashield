
# createUser: POST /user
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user$" "id:4200484,phase:2,pass,nolog,skipAfter:END_createUser"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200485,phase:2,pass,nolog,skipAfter:END_createUser"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:json\.id|json\.username|json\.firstName|json\.lastName|json\.email|json\.password|json\.phone|json\.userStatus)$" "id:4200496,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4200498,phase:2,pass,nolog,skipAfter:ENDJSON_createUser"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4200499,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.id "!@rx ^[0-9]{1,19}$" "id:4210906,phase:2,block,msg:'Invalid value for property json.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.username "!@rx ^.+$" "id:4210918,phase:2,block,msg:'Invalid value for property json.username',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.firstName "!@rx ^.+$" "id:4210930,phase:2,block,msg:'Invalid value for property json.firstName',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.lastName "!@rx ^.+$" "id:4210942,phase:2,block,msg:'Invalid value for property json.lastName',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.email "!@rx ^.+$" "id:4210954,phase:2,block,msg:'Invalid value for property json.email',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.password "!@rx ^.+$" "id:4210966,phase:2,block,msg:'Invalid value for property json.password',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.phone "!@rx ^.+$" "id:4210978,phase:2,block,msg:'Invalid value for property json.phone',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.userStatus "!@rx ^[0-9]{1,19}$" "id:4210990,phase:2,block,msg:'Invalid value for property json.userStatus',log,auditlog,skipAfter:FAILED_API_CHECKS"
# Coraza implements @validateSchema for JSON Schema; ModSecurity3's is XSD-only,
# which is why the modsecurity3 flavor relies on the per-field rules above.
SecRule REQUEST_BODY "@validateSchema schema.json" "id:4200500,phase:2,block,msg:'JSON schema validation failed for createUser',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4200501,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_createUser"

SecMarker ENDJSON_createUser


# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200503,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_createUser

## The request passed all checks
SecAction "phase:2,allow:request,id:4200504"

## End of checks for this operation
SecMarker END_createUser

# createUsersWithArrayInput: POST /user/createWithArray
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/createWithArray$" "id:4200524,phase:2,pass,nolog,skipAfter:END_createUsersWithArrayInput"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200525,phase:2,pass,nolog,skipAfter:END_createUsersWithArrayInput"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200536,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4200538,phase:2,pass,nolog,skipAfter:ENDJSON_createUsersWithArrayInput"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4200539,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
# Coraza implements @validateSchema for JSON Schema; ModSecurity3's is XSD-only,
# which is why the modsecurity3 flavor relies on the per-field rules above.
SecRule REQUEST_BODY "@validateSchema schema.json" "id:4200540,phase:2,block,msg:'JSON schema validation failed for createUsersWithArrayInput',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4200541,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_createUsersWithArrayInput"

SecMarker ENDJSON_createUsersWithArrayInput


# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200543,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_createUsersWithArrayInput

## The request passed all checks
SecAction "phase:2,allow:request,id:4200544"

## End of checks for this operation
SecMarker END_createUsersWithArrayInput

# createUsersWithListInput: POST /user/createWithList
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/createWithList$" "id:4200564,phase:2,pass,nolog,skipAfter:END_createUsersWithListInput"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within POST" "id:4200565,phase:2,pass,nolog,skipAfter:END_createUsersWithListInput"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200576,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4200578,phase:2,pass,nolog,skipAfter:ENDJSON_createUsersWithListInput"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4200579,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
# Coraza implements @validateSchema for JSON Schema; ModSecurity3's is XSD-only,
# which is why the modsecurity3 flavor relies on the per-field rules above.
SecRule REQUEST_BODY "@validateSchema schema.json" "id:4200580,phase:2,block,msg:'JSON schema validation failed for createUsersWithListInput',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4200581,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_createUsersWithListInput"

SecMarker ENDJSON_createUsersWithListInput


# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200583,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_createUsersWithListInput

## The request passed all checks
SecAction "phase:2,allow:request,id:4200584"

## End of checks for this operation
SecMarker END_createUsersWithListInput

# deleteUser: DELETE /user/{username}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/(?:.+)$" "id:4200604,phase:2,pass,nolog,skipAfter:END_deleteUser"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within DELETE" "id:4200605,phase:2,pass,nolog,skipAfter:END_deleteUser"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200616,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200627,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_deleteUser"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200623,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_deleteUser

## The request passed all checks
SecAction "phase:2,allow:request,id:4200624"

## End of checks for this operation
SecMarker END_deleteUser

# getUserByName: GET /user/{username}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/(?:.+)$" "id:4200644,phase:2,pass,nolog,skipAfter:END_getUserByName"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200645,phase:2,pass,nolog,skipAfter:END_getUserByName"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200656,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200667,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_getUserByName"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200663,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_getUserByName

## The request passed all checks
SecAction "phase:2,allow:request,id:4200664"

## End of checks for this operation
SecMarker END_getUserByName

# loginUser: GET /user/login
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/login$" "id:4200684,phase:2,pass,nolog,skipAfter:END_loginUser"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200685,phase:2,pass,nolog,skipAfter:END_loginUser"

SecRule ARGS_GET:username "!@rx /^[a-zA-Z0-9]+[a-zA-Z0-9\\.\\-_]*[a-zA-Z0-9]+$/" "id:4211206,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:username "@gt 1" "id:4211207,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS_GET:password "!@rx ^.+$" "id:4211246,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:password "@gt 1" "id:4211247,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:username|password)$" "id:4200696,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200707,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_loginUser"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200703,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_loginUser

## The request passed all checks
SecAction "phase:2,allow:request,id:4200704"

## End of checks for this operation
SecMarker END_loginUser

# logoutUser: GET /user/logout
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/logout$" "id:4200724,phase:2,pass,nolog,skipAfter:END_logoutUser"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200725,phase:2,pass,nolog,skipAfter:END_logoutUser"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:)$" "id:4200736,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200747,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_logoutUser"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200743,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_logoutUser

## The request passed all checks
SecAction "phase:2,allow:request,id:4200744"

## End of checks for this operation
SecMarker END_logoutUser

# updateUser: PUT /user/{username}
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/user/(?:.+)$" "id:4200764,phase:2,pass,nolog,skipAfter:END_updateUser"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within PUT" "id:4200765,phase:2,pass,nolog,skipAfter:END_updateUser"


# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:json\.id|json\.username|json\.firstName|json\.lastName|json\.email|json\.password|json\.phone|json\.userStatus)$" "id:4200776,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models
SecRule REQUEST_HEADERS:Content-Type "!@rx ^application/json" "id:4200778,phase:2,pass,nolog,skipAfter:ENDJSON_updateUser"
# ModSecurity3 flags unparseable bodies here; Coraza does not set REQBODY_ERROR,
# but its @validateSchema rule below rejects malformed JSON instead.
SecRule REQBODY_ERROR "!@eq 0" "id:4200779,phase:2,block,msg:'Failed to parse request body',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.id "!@rx ^[0-9]{1,19}$" "id:4211322,phase:2,block,msg:'Invalid value for property json.id',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.username "!@rx ^.+$" "id:4211334,phase:2,block,msg:'Invalid value for property json.username',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.firstName "!@rx ^.+$" "id:4211346,phase:2,block,msg:'Invalid value for property json.firstName',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.lastName "!@rx ^.+$" "id:4211358,phase:2,block,msg:'Invalid value for property json.lastName',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.email "!@rx ^.+$" "id:4211370,phase:2,block,msg:'Invalid value for property json.email',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.password "!@rx ^.+$" "id:4211382,phase:2,block,msg:'Invalid value for property json.password',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.phone "!@rx ^.+$" "id:4211394,phase:2,block,msg:'Invalid value for property json.phone',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS:json.userStatus "!@rx ^[0-9]{1,19}$" "id:4211406,phase:2,block,msg:'Invalid value for property json.userStatus',log,auditlog,skipAfter:FAILED_API_CHECKS"
# Coraza implements @validateSchema for JSON Schema; ModSecurity3's is XSD-only,
# which is why the modsecurity3 flavor relies on the per-field rules above.
SecRule REQUEST_BODY "@validateSchema schema.json" "id:4200780,phase:2,block,msg:'JSON schema validation failed for updateUser',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecAction "id:4200781,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_updateUser"

SecMarker ENDJSON_updateUser


# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200783,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_updateUser

## The request passed all checks
SecAction "phase:2,allow:request,id:4200784"

## End of checks for this operation
SecMarker END_updateUser
