
# findPetsByStatus: GET /pet/findByStatus
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/pet/findByStatus$" "id:4200002,phase:2,pass,nolog,skipAfter:END_findPetsByStatus"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200003,phase:2,pass,nolog,skipAfter:END_findPetsByStatus"

SecRule ARGS_GET:status "!@rx ^(available|pending|sold)$" "id:4210006,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:status "@lt 1" "id:4210008,phase:2,block,msg:'Too few values for array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:status)$" "id:4200014,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200025,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_findPetsByStatus"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200021,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_findPetsByStatus

## The request passed all checks
SecAction "phase:2,allow:request,id:4200022"

## End of checks for this operation
SecMarker END_findPetsByStatus

# findPetsByTags: GET /pet/findByTags
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/pet/findByTags$" "id:4200042,phase:2,pass,nolog,skipAfter:END_findPetsByTags"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200043,phase:2,pass,nolog,skipAfter:END_findPetsByTags"

SecRule ARGS_GET:tags "!@rx ^.+$" "id:4210046,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:tags "@lt 1" "id:4210048,phase:2,block,msg:'Too few values for array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:tags)$" "id:4200054,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200065,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_findPetsByTags"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200061,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_findPetsByTags

## The request passed all checks
SecAction "phase:2,allow:request,id:4200062"

## End of checks for this operation
SecMarker END_findPetsByTags

# listPets: GET /pets
# Skip this operation if the request does not match the operation path.
# Path parameter validation patterns are embedded in the regex, so this rule
# both routes and validates path parameters (works on ModSecurity3 and Coraza).
SecRule REQUEST_FILENAME "!@rx ^/pets$" "id:4200082,phase:2,pass,nolog,skipAfter:END_listPets"
# Skip this operation if the request method does not match the operation
SecRule REQUEST_METHOD "!@within GET" "id:4200083,phase:2,pass,nolog,skipAfter:END_listPets"

SecRule ARGS_GET:limit "!@rx ^([0-9]{1,19})?$" "id:4210086,phase:2,block,msg:'Forbidden parameter value detected',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule &ARGS_GET:limit "@gt 1" "id:4210087,phase:2,block,msg:'Multiple values for non-array parameter',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS_GET:limit "@lt 1" "id:4210096,phase:2,block,msg:'Parameter value below minimum',log,auditlog,skipAfter:FAILED_API_CHECKS"
SecRule ARGS_GET:limit "@gt 100" "id:4210097,phase:2,block,msg:'Parameter value above maximum',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Reject unknown parameters: ARGS_NAMES holds query, form, and flattened JSON body
# names on both engines, so one allowlist covers them all.
SecRule ARGS_NAMES "!@rx ^(?:limit)$" "id:4200094,phase:2,block,msg:'Unknown parameter detected',log,auditlog,skipAfter:FAILED_API_CHECKS"

# Handle Models

SecAction "id:4200105,phase:2,pass,nolog,skipAfter:AFTER_CONSUMES_listPets"

# Action has consumes, but it wasn't matched - validation must have failed for both JSON and XML
SecAction "id:4200101,log,auditlog,block,phase:2,msg:'Body processing failed - invalid data content?'"


SecMarker AFTER_CONSUMES_listPets

## The request passed all checks
SecAction "phase:2,allow:request,id:4200102"

## End of checks for this operation
SecMarker END_listPets
